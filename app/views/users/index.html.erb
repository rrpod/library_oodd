<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title></title>
</head>
<body>
<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
    <% @user.each do |user| %>
    <li>

        <%= link_to user.first_name, user %>
        <% if current_user && current_user.admin? %>
          <%= link_to 'Details', user, method: 'show'%>
          <%= link_to 'Delete', user , method: 'delete' , data: { confirm: "You sure?" } %>
        <% end %>
    </li>
    <% end %>

</ul>
</body>
</html>